<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manage.dao.DeviceManageDao">
  <select id="selectDeviceFromHouseId" resultType="com.house.entity.Device" parameterType="java.lang.String">
  select * from device where house_id = #{parameter}
  </select>
</mapper>